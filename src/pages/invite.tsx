import React from "react";
import Layout from "../components/layout/Layout";

const Invite = () => {
    return (
        <Layout>
            <div className="p-6 w-full max-w-6xl mx-auto mt-5">
                <h1>No Invite ID Attached </h1>
            </div>
        </Layout>
    );
};

export default Invite;
